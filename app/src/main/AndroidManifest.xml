<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.deedash.firecrud">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FirebaseCrudApp">
        <activity
            android:name="com.deedash.firecrud.EditCourseActivity"
            android:label="Edit Course" />
        <activity
            android:name="com.deedash.firecrud.AddCourseActivity"
            android:label="Add Course" />
        <activity android:name="com.deedash.firecrud.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.deedash.firecrud.MainActivity" />
    </application>

</manifest>